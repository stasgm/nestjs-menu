version: '3.8'
services:

  postgres:
    image: postgres:13.5
    restart: always
    env_file:
      .env
    volumes:
      - /home/stas/workspace/postgres_data:/var/lib/postgresql/data
    ports:
      - '5433:5432'

volumes:
  postgres:
    name: postgres-data